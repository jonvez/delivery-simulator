# Loaded automatically by Vite in production builds (`vite build` / `npm run build`).
# Labels Datadog RUM telemetry for the deployed demo (ADR 0008, item #8).
# NOTE: VITE_API_BASE_URL is intentionally NOT set here — it is provided at
# deploy time (it's the sslip.io backend URL) so it stays out of git.
VITE_DD_ENV=production
